database folder has an SQL file which is imported to the phpmyAdmin localhost or any other alternative. 
Laravel_app has the app itself which runs when the localhost servers are on, the vite server is on (USE THE COMMAND: npm run dev) and the php artisan server is on (USE THE COMMAND: php artisan serve), the homepage is viewed on the url localhost:8000.
